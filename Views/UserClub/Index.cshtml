@model IEnumerable<GB.Models.UserClub>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            Brand
        </th>
        <th>
            Name
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Brand)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UserClubID }) |
            @Html.ActionLink("Details", "Details", new { id = item.UserClubID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.UserClubID })
        </td>
    </tr>
}

</table>
