@model IEnumerable<GB.Models.GolfClub>
<tr>
    <th>
        Logo
    </th>
    <th>
        Name
    </th>
    <th>
    </th>
</tr>
@foreach (var item in Model)
{
    <tr>
        <td>
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.GolfClubID }) |
            @Html.ActionLink("Details", "Details", new { id = item.GolfClubID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.GolfClubID })
        </td>
    </tr>
}
